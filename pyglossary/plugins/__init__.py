"""
PyGlossary format plugins package.

Each subdirectory implements one dictionary interchange format with optional
``Reader`` and ``Writer`` classes. ``PluginHandler`` discovers these modules at
runtime from this package tree.
"""
