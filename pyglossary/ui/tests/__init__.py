"""
Unit tests for the :mod:`pyglossary.ui` package.

Covers CLI parsing, terminal theming, and interactive command-line UI behavior.
Run via ``scripts/test.sh`` or ``uv run pytest pyglossary/ui/tests``.
"""
